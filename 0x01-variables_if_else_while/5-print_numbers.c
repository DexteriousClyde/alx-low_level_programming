#include <stdio.h>

/**
 * main - main running code
u * Return: 0
 */

int main(void)
{
	int letter;

	for (letter = 0; letter <= 9; letter++)
	{
		printf("%d", letter);
	}
	printf('\n');
	return (0);
}
